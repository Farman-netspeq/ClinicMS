namespace ClinicMS.Application.DTOs.Appointment
{
    // Used for Check-in, Complete, NoShow — no extra fields needed beyond the id (from route)
    // Kept as marker class in case status-specific notes get added later
    public class StatusUpdateDto
    {
    }
}